# =============================================================
# .env は「秘密情報」と「読み込む設定ファイルの選択」だけを持つ。
# リレー / 有効化フラグ / cron などの非機密設定は config.json (JSON) で管理する。
# =============================================================

# 読み込む設定ファイルを切り替える。
#   未指定        -> config.json
#   APP_ENV=LOCAL -> config.local.json
# APP_ENV="LOCAL"

# -------------------------------------------------------------
# 必須
# -------------------------------------------------------------
# Bot のメイン秘密鍵 (hex 64 文字)
HEX=""

# -------------------------------------------------------------
# CalendarBot 用 LLM (任意。あると高精度解析)
# -------------------------------------------------------------
# OPENROUTER_API_KEY="sk-or-v1-..."
# OPENAI_API_KEY="sk-..."

# -------------------------------------------------------------
# MonitorBot 用 Discord Webhook (monitor.enabled=true なら必須)
# -------------------------------------------------------------
# DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/..."

# -------------------------------------------------------------
# IoTBot / SwitchBot (実機操作するなら必須)
# -------------------------------------------------------------
# SWITCH_BOT_TOKEN=""
# SWITCH_BOT_SECRET=""

# -------------------------------------------------------------
# MetadataRefreshJob 対象アカウントの秘密鍵 (nsec or hex, カンマ区切り)
# (metadataRefresh.enabled=true なら必須)
# -------------------------------------------------------------
# METADATA_KEYS="nsec1...,nsec1..."

# -------------------------------------------------------------
# PassportJob 送信用の秘密鍵 (nsec or hex)
# (passport.enabled=true なら必須)
# -------------------------------------------------------------
# PASSPORT_KEY=""
