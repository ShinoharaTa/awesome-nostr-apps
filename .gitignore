node_modules/
dist/
*.log
log/
.env
.env.local
.DS_Store

# src/config/config.ts は Git 管理（機密は含めない）。ローカル上書きのみ無視する
src/config/config.local.ts
# 旧 JSON 設定（config.ts へ移行済み。残っていても無視）
config.json
config.local.json

# 既存リポジトリへのシンボリックリンク (参照専用・このプロジェクトには含めない)
/awesome-bot
/NostrIot
/OnlineConcierge
/nostr-flowmeter-batch
/nostr-metadata-enhancer
